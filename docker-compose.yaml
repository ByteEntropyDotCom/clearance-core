version: '3.8'

services:
  clearance-core:
    build: .
    container_name: clearance-core-service
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:h2:mem:clearancedb
      - SPRING_PROFILES_ACTIVE=docker
    restart: unless-stopped